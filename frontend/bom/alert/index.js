/**
 * Method alert() merupakan salah satu method BOM yang menerima
 * satu parameter opsional berupa data string yang akan 
 * ditampilkan melalui sebuah pop-up browser. Contohnya, jika
 * kita ingin menampilkan sebuah pesan berisi string "Halo, user!"
 * menggunakan console browser, caranya sebagai berikut:
 */


let message = 'Hallo, user!'
alert(message);